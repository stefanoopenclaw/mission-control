# Mission Control

Productivity dashboard with OpenClaw integrations.

## Setup
1. `npm install`
2. `npm run dev` – Runs on http://localhost:3000

## Features
- Task list management
- OpenClaw tool proxies (calendar, email, etc.)

## Extend
- Add auth (NextAuth.js)
- Integrate real APIs (Google Calendar via OAuth)
